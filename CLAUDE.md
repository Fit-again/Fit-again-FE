# 협업 규칙

## Git 작업

- 이슈 생성, 브랜치 생성, 커밋, 푸시와 Pull Request 생성은 사용자가 직접 진행한다.
- 에이전트는 이슈, 브랜치, 커밋, 푸시나 Pull Request를 직접 생성하지 않는다.
- 에이전트는 매 작업이 끝날 때 변경 내용에 알맞은 커밋 제목을 추천한다.
- 커밋 제목은 한글로 작성한다.
- `docs:`, `feat:`, `fix:`, `chore:` 등의 커밋 태그는 영어로 작성한다.

## 작업 진행

- 에이전트는 작업을 시작하기 전에 진행할 작업과 변경 범위를 사용자에게 설명한다.
- 에이전트는 반드시 사용자의 명시적인 허락을 받은 후 작업을 시작한다.
- 에이전트는 매 작업을 시작할 때 루트의 `AGENTS.md`와 `README.md`를 모두 읽는다.

## 브랜치 단위 작업

- 이 문서에서 작업은 하나의 브랜치에서 처리하는 큰 작업 단위를 의미한다.
- 에이전트는 합의된 이슈와 브랜치의 작업 범위를 넘어서는 기능이나 변경을 임의로 진행하지 않는다.
- 현재 브랜치 범위 밖의 작업이 필요하거나 발견되면 구현하지 않고 사용자에게 별도 이슈와 브랜치 작업으로 제안한다.
- 브랜치를 만들기 전 작업 종류에 맞는 `.github/ISSUE_TEMPLATE` 파일을 읽는다.
- 에이전트는 이슈 템플릿 형식에 맞춘 이슈 제목과 이슈 내용을 사용자에게 제공한다.
- 에이전트는 이슈 범위와 Git 협업 전략에 맞는 브랜치 이름을 함께 추천한다.
- 브랜치의 구현과 최종 검증이 모두 끝난 후 `.github/pull_request-template.md`를 읽는다.
- 에이전트는 실제 변경 내용과 검증 결과를 반영한 PR 제목과 PR 내용을 템플릿 형식에 맞춰 사용자에게 제공한다.
- 이슈 번호가 확정되지 않은 경우 PR 내용에는 `#이슈번호` 자리 표시자를 유지한다.

## 개발 기준

- `jeongbiseo-frontend` 프로젝트를 Fit Again 프론트엔드 개발의 베이스로 삼는다.
- 코드 스타일, 작성 방식, 디렉토리 구조와 구현 방법은 `jeongbiseo-frontend`의 방식을 최대한 차용하고 Fit Again 서비스에 맞게 응용한다.
- 참고 프로젝트의 코드를 그대로 복사해 붙여 넣지 않는다.
- 기능과 도메인 요구사항은 Fit Again 서비스를 기준으로 설계하고 구현한다.
